server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ushan-Gimhan/agms-config-repo.git
          default-label: main # GitHub branch 'main'
          force-pull: true # GitHub